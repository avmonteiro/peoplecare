<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'PeopleCare', root_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <%= form_tag(root_path, method: "get") do %>
            <%= text_field_tag 'search', nil, placeholder: 'Ex: nutricionista...', class: 'form-control' %>
            <%= submit_tag "Buscar", class: 'btn btn-default' %>
          <% end %>
        </div>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% if professional_signed_in? %>

          <li class="navbar-text">Olá <%= current_professional.email%></li>

          <li><%= link_to 'Pacientes Próximos', nearby_patients_path, class: 'btn btn-info navbar-btn' %></li>
          <li><%= link_to 'Logout', destroy_professional_session_path, :method => :delete, class: 'btn btn-info navbar-btn' %></li>

        <% end %>

        <% if patient_signed_in? %>
            <li class="navbar-text">Olá <%= current_patient.email %></li>
            <li><%= link_to 'Profissionais Próximos', nearby_professionals_path, class: 'btn btn-info navbar-btn' %></li>
            <li><%= link_to 'Logout', destroy_patient_session_path, :method => :delete, class: 'btn btn-info navbar-btn' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
